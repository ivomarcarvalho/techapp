import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "100%",
        height: 52,
        borderColor: "red",
        borderWidth: 1,
        borderRadius: 10,
        padding:12,
    }
})